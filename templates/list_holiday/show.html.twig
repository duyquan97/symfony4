{% extends 'base.html.twig' %}

{% block title %}ListHoliday{% endblock %}

{% block body %}
    <h1>ListHoliday</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ list_holiday.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ list_holiday.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ list_holiday.date ? list_holiday.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('list_holiday_index') }}">back to list</a>

    <a href="{{ path('list_holiday_edit', {'id': list_holiday.id}) }}">edit</a>

    {{ include('list_holiday/_delete_form.html.twig') }}
{% endblock %}
